{
  "name": "module-federation",
  "version": "1.0.0",
  "private": "true",
  "workspaces": {
    "packages": [
      "packages/*"
    ],
      "nohoist": ["**/sass", "webpack-test/*"]
  },
  "scripts": {
    "start": "lerna run --parallel start",
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "link": "lerna link",
    "publish": "lerna publish --no-push",
    "init": "lerna init --independent",
    "verdaccio": "verdaccio",
    "add": "lerna add app1 --scope=app3",
    "clean": "lerna clean"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
